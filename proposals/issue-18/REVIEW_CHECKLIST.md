# Issue #18 — independent R checklist

Review the **complete snapshots** in `target/` against fixed baseline `agenti@99a0f90d3c7a558fc9fea375e20252374b4726a1` and canonical A report `5727352795` as approved by re-review `5727558241`.

Minimum acceptance checks:

1. Canonical authority/delivery roles are exactly H/A/D/R/P.
2. Removed former roles do not survive as aliases, hidden canonical roles or lost responsibilities.
3. D owns author validation; deterministic tests remain gates; required independent behavioral verification remains possible as R activity/run.
4. O owns mechanical multi-repo composite binding/revalidation and remains the **only** dispatcher.
5. Asistentka cannot derive work contracts or select next role.
6. P executes exact already-approved/authorized publication only; P cannot grant release authorization, waive gates, repair implementation or substitute candidate.
7. H release authorization remains exact-bound and distinct from R technical approval/P execution.
8. R keeps mandatory independence from D/author.
9. Final Done remains O mechanical close-out only after all configured publication/post-publication conditions.
10. Human conflict/override protocol requires durable bound state mutation before O resume; private chat/handoff is not bypass authority.
11. Events/webhooks/Actions outputs are triggers/evidence, not authority or durable governance truth.
12. `GITHUB_TOKEN` guidance includes the approved narrow approval-gated PR-event exception and does not use it as role chaining.
13. Concurrency guidance includes current `queue: single` / `queue: max` / `cancel-in-progress` semantics and still treats concurrency only as a guard.
14. External webhook O does not rely on arrival order and includes delivery-ID dedup/fresh reconstruction/retry-redelivery handling.
15. Actions-centric O remains viable for small single-repo; hybrid App/webhook O is only the robust full/multi-repo reference, not a universal infrastructure mandate.
16. GitHub Agentic Workflows are clearly optional public preview runner adapters.
17. Untrusted issue/comment/PR text cannot become privileged instruction; D/untrusted execution is separated from P credentials.
18. P1 semantic authority/effective-state/implementation-fidelity and lossless supersession from baseline remain intact.
19. Exact-candidate/stale, Blocked/Stopped, Parent Intent, earliest-affected-point, objective-progress and run/write idempotence semantics are not weakened.
20. No unrelated scope, provider mandate, new backlog/database, new lifecycle family or lab-governance change was introduced.

Record exactly one outcome against the exact PR head: `APPROVED`, `CHANGES_REQUIRED` or `DECISION_REQUIRED`.
